# BFF runtime
PORT=3000
NODE_ENV=development

# Bootstrap mode: auto | manual
LC_DB_BOOTSTRAP_MODE=manual

# Database URLs (single PostgreSQL instance with separated databases)
LC_DB_META_URL=
LC_DB_BUSINESS_URL=
LC_DB_AUDIT_URL=

# Safety guard
LC_MIGRATION_ALLOW_DESTRUCTIVE=false
